{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.6.1"
  },
  "scripts": {
    "start": "lerna run start",
    "web": "lerna run start --scope=web",
    "api-server": "lerna run start --scope=api-server",
    "ios": "lerna run ios --scope=mobileapp",
    "android": "lerna run android --scope=mobileapp",
    "test": "lerna run test",
    "dev": "lerna run dev",
    "diff": "lerna diff"
  },
  "dependencies": {}
}
